{% extends "index" %}

{% block content %}

    <div class="p-2 sm:p-5">

        <div class="flex justify-center"> {% include "components/globals/teaser" %} </div>

        <div> {% include "components/globals/faktenbox" %} </div>

    </div>



                    {# Include Pages #}
                {# {% include "components/index/pages" %} #}

                {# Include Links #}
                {% include "components/index/links" %}

    {# Title of Website (in mobile version hidden) #}
    {% include "components/home/header" %}

    {# An infobox with some current changeable data #}
    {% include "components/home/infobox" %}

    {# The newsfeed #}
    {% include "components/home/newsfeed" %}

{% endblock %}